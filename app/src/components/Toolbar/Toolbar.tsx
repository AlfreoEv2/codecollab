import "./Toolbar.css";

const Toolbar = () => {
  return (
    <div className="toolbar">
      <button>File</button>
      <button>Edit</button>
      <button>View</button>
      <button>Run</button>
    </div>
  );
};

export default Toolbar;
